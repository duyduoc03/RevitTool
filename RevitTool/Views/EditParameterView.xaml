<Window
    x:Class="RevitTool.Views.EditParameterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:viewModel="clr-namespace:RevitTool.ViewModels;assembly=RevitTool"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewModel:EditParameterViewModel, IsDesignTimeCreatable=False}"
    WindowStartupLocation="CenterScreen"
    Title="{Binding Title}"
    Height="500"
    Width="600">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid
            Grid.Row="0"
            ItemsSource="{Binding Parameters}"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            HeadersVisibility="Column"
            Margin="0,0,0,15"
            BeginningEdit="DataGrid_BeginningEdit">

            <DataGrid.Columns>

                <DataGridTextColumn
                    Header="Parameter"
                    Binding="{Binding Name}"
                    Width="2*"
                    IsReadOnly="True" />

                <DataGridTextColumn
                    Header="Value"
                    Binding="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                    Width="2*">
                    <DataGridTextColumn.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsReadOnly}" Value="True">
                                    <Setter Property="Background" Value="#F0F0F0"/>
                                    <Setter Property="Foreground" Value="Gray"/>
                                    <Setter Property="Focusable" Value="False"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.CellStyle>
                </DataGridTextColumn>

                <DataGridTextColumn
                    Header="Type"
                    Binding="{Binding StorageType}"
                    Width="*"
                    IsReadOnly="True" />

                <DataGridCheckBoxColumn
                    Header="Read Only"
                    Binding="{Binding IsReadOnly}"
                    Width="*"
                    IsReadOnly="True" />

            </DataGrid.Columns>
        </DataGrid>

        <Button
            Grid.Row="1"
            HorizontalAlignment="Right"
            Width="100"
            Height="32"
            Content="Apply"
            Command="{Binding ApplyCommand}"
            Background="#1976D2"
            BorderBrush="#1976D2"
            Foreground="White" />

    </Grid>
</Window>
